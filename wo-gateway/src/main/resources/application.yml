server:
  port: 8080

spring:
  application:
    name: wo-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.213.100:8848
    sentinel:
      transport:
        dashboard: 192.168.213.100:8080
  config:
    import: optional:nacos:wo-gateway.yml

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
